import styled from "styled-components";
import {fetchCoins} from "../api";

function Coins() {
    return (
        <Container>
            <Helmet>
                <title>Coins!</title>
            </Helmet>
        </Container>
    );
}

export default Coins;


